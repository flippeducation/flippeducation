<nav id="menu" class="custom-wrapper pure-g">
	<div class="pure-u-1">
		<div class="pure-menu pure-menu-horizontal custom-can-transform">
			<a class="pure-menu-heading custom-brand" href="/">flippeducation</a>
			<a id="toggle" class="custom-toggle">
				<s class="bar"></s>
				<s class="bar"></s>
			</a>
			<ul id="menu-items" class="pure-menu-list"><%
				for (const [path, {navbar, title: tl, navbarTitle}] of pages) {
					if (navbar) {
						if (path === page && !(query && query.success)) {
						%><li class="pure-menu-item pure-menu-selected">
							<a class="pure-menu-link" href="#">
								<%= __(tl || navbarTitle) %>
							</a>
						</li><%
						} else {
						%><li class="pure-menu-item">
							<a class="pure-menu-link" href="<%= path %>">
								<%= __(tl || navbarTitle) %>
							</a>
						</li><%
						}
					}
				}
			%></ul>
		</div>
	</div>
</nav>
